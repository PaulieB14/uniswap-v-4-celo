{
  "name": "uniswap-v-4-celo",
  "version": "1.0.0",
  "repository": "https://github.com/PaulieB14/uniswap-v-4-celo",
  "license": "GPL-3.0-or-later",
  "scripts": {
    "build": "graph codegen --output-dir src/types/ && graph build",
    "codegen": "graph codegen --output-dir src/types/",
    "test": "graph test -d",
    "generate-subgraph": "ts-node scripts/generate-subgraph.ts"
  },
  "devDependencies": {
    "@graphprotocol/graph-cli": "^0.64.1",
    "@graphprotocol/graph-ts": "^0.32.0",
    "matchstick-as": "^0.6.0",
    "typescript": "^3.5.2"
  },
  "dependencies": {
    "yaml": "^2.3.4"
  }
}
